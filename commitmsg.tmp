feat(admin+widget): self-serve Knowledge/Settings pages; one chat color everywhere

Admin (Shopify staff only):
- Knowledge page: upload manuals (paste or file) and remove them; commits to
  GitHub via API, which auto-rebuilds the index and deploys (needs
  GITHUB_TOKEN env var - fine-grained PAT with Contents permission)
- Settings page: rotate CLAUDE_API_KEY / VOYAGE_API_KEY / ALLOWED_ORIGINS,
  write-only, updates Vercel env + auto-redeploys (needs VERCEL_TOKEN env var)

Widget (theme editor):
- the Chat color setting now drives everything (floating bubble, header,
  customer bubbles, links, buttons) via a CSS variable + auto-darkened hover,
  fixing the bubble vs in-chat color mismatch
- new settings: position (bottom left or right) and chat window title

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
